import styled from 'styled-components';

export const Content = styled.div`
    background: #001529;

    .header {
        display: flex;
        align-items: center;
        padding: 0 24px;
        max-width: 1280px;
        margin: 0 auto;

        img {
            max-width: 120px;
        }
    }
`;